<nav class="navbar custom-nav">
  <div class="container-fluid container">
    <div class="navbar-header col-md-2">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>                        
      </button>
      <div class="navbar-brand">
        <%= link_to 'B.', root_path %>
      </div>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Articles", articles_path, class: cp('/articles') %></li>
        <li><%= link_to "Users", users_path, class: cp('/users') %></li>
        <% if logged_in? %>
          <li class="user-dropdown">
            <span class="fa fa-user" data-toggle="dropdown"></span>
            <ul class="dropdown-menu">
              <li><%= link_to "View Profile", user_path(current_user) %></li>
              <li><%= link_to "Edit Account", edit_user_path(current_user) %></li>
              <li><%= link_to "Logout", logout_path, method: :delete, class: 'danger-text' %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Sign up", signup_path, class: cp('/signup') %></li>
          <li><%= link_to "Login", login_path, class: cp('/login') %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>